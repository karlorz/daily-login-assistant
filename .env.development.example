# Local Development Environment Variables

# SSH Tunnel Configuration
SSH_TUNNEL_PASSWORD=dev
REMOTE_SSH_HOST=localhost
REMOTE_SSH_PORT=2222
REMOTE_SSH_USER=tunnel

# Web API Configuration
WEB_API_PORT=3001
API_ENDPOINT=http://localhost:3001/api/tunnel-ready
ALLOWED_ORIGIN=http://localhost:3001

# Webhook configuration for development
WEBHOOK_PORT=3001

# Option 1: Logger service (works but hard to verify - no visible output)
# NOTIFICATION_URLS=logger://

# Option 2: Generic webhook to integrated listener (visible notifications in console)
NOTIFICATION_URLS="generic://localhost:3001?disabletls=yes&template=json"

# Option 3: Use multiple services for development - get both logs and webhook
# NOTIFICATION_URLS=logger://,generic://localhost:3001

# Locale settings
LANG=C.UTF-8
LC_ALL=C.UTF-8
TZ=Asia/Hong_Kong

# Application settings
NODE_ENV=development
LOG_LEVEL=debug

# Website Credentials (Optional - for legacy auto-fill method)
# NOTE: PWA user-guided login (primary method) doesn't require these
# These are only needed if using the legacy auto-fill credentials method
# SITE1_USERNAME=your_username
# SITE1_PASSWORD=your_password